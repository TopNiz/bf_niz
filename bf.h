#include<stdio.h>
#include<stdlib.h>

#define GT      ptr++;
#define LT      ptr--;
#define PLUS    ++(*ptr);
#define MINUS   --(*ptr);
#define OBRA    while(*ptr) {
#define CBRA    }
#define DOT     putchar(*ptr);
